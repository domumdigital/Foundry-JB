*** BUILT BY DOMUM DIGITAL. Est. 2021. ***

*** TO REINSTALL EXTERNAL LIBRARIES ***

In Project Folder:
    ```forge install smartcontractkit/chainlink-brownie-contracts --no-commit```

In /lib:
    ```forge install foundry-rs/forge-std --no-commit```
